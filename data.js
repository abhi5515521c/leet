const allProblems = [
    [1, "Two Sum", "Easy"], [2, "Add Two Numbers", "Medium"], [3, "Longest Substring Without Repeating Characters", "Medium"],
    [4, "Median of Two Sorted Arrays", "Hard"], [5, "Longest Palindromic Substring", "Medium"], [6, "Zigzag Conversion", "Medium"],
    [7, "Reverse Integer", "Medium"], [8, "String to Integer (atoi)", "Medium"], [9, "Palindrome Number", "Easy"],
    [10, "Regular Expression Matching", "Hard"], [11, "Container With Most Water", "Medium"], [12, "Integer to Roman", "Medium"],
    [13, "Roman to Integer", "Easy"], [14, "Longest Common Prefix", "Easy"], [15, "3Sum", "Medium"],
    [16, "3Sum Closest", "Medium"], [17, "Letter Combinations of a Phone Number", "Medium"], [18, "4Sum", "Medium"],
    [19, "Remove Nth Node From End of List", "Medium"], [20, "Valid Parentheses", "Easy"], [21, "Merge Two Sorted Lists", "Easy"],
    [22, "Generate Parentheses", "Medium"], [23, "Merge k Sorted Lists", "Hard"], [24, "Swap Nodes in Pairs", "Medium"],
    [25, "Reverse Nodes in k-Group", "Hard"], [26, "Remove Duplicates from Sorted Array", "Easy"], [27, "Remove Element", "Easy"],
    [28, "Find the Index of the First Occurrence in a String", "Easy"], [29, "Divide Two Integers", "Medium"], [30, "Substring with Concatenation of All Words", "Hard"],
    [31, "Next Permutation", "Medium"], [32, "Longest Valid Parentheses", "Hard"], [33, "Search in Rotated Sorted Array", "Medium"],
    [34, "Find First and Last Position of Element in Sorted Array", "Medium"], [35, "Search Insert Position", "Easy"], [36, "Valid Sudoku", "Medium"],
    [37, "Sudoku Solver", "Hard"], [38, "Count and Say", "Medium"], [39, "Combination Sum", "Medium"],
    [40, "Combination Sum II", "Medium"], [41, "First Missing Positive", "Hard"], [42, "Trapping Rain Water", "Hard"],
    [43, "Multiply Strings", "Medium"], [44, "Wildcard Matching", "Hard"], [45, "Jump Game II", "Medium"],
    [46, "Permutations", "Medium"], [47, "Permutations II", "Medium"], [48, "Rotate Image", "Medium"],
    [49, "Group Anagrams", "Medium"], [50, "Pow(x, n)", "Medium"], [51, "N-Queens", "Hard"],
    [52, "N-Queens II", "Hard"], [53, "Maximum Subarray", "Medium"], [54, "Spiral Matrix", "Medium"],
    [55, "Jump Game", "Medium"], [56, "Merge Intervals", "Medium"], [57, "Insert Interval", "Medium"],
    [58, "Length of Last Word", "Easy"], [59, "Spiral Matrix II", "Medium"], [60, "Permutation Sequence", "Hard"],
    [61, "Rotate List", "Medium"], [62, "Unique Paths", "Medium"], [63, "Unique Paths II", "Medium"],
    [64, "Minimum Path Sum", "Medium"], [65, "Valid Number", "Hard"], [66, "Plus One", "Easy"],
    [67, "Add Binary", "Easy"], [68, "Text Justification", "Hard"], [69, "Sqrt(x)", "Easy"],
    [70, "Climbing Stairs", "Easy"], [71, "Simplify Path", "Medium"], [72, "Edit Distance", "Medium"],
    [73, "Set Matrix Zeroes", "Medium"], [74, "Search a 2D Matrix", "Medium"], [75, "Sort Colors", "Medium"],
    [76, "Minimum Window Substring", "Hard"], [77, "Combinations", "Medium"], [78, "Subsets", "Medium"],
    [79, "Word Search", "Medium"], [80, "Remove Duplicates from Sorted Array II", "Medium"], [81, "Search in Rotated Sorted Array II", "Medium"],
    [82, "Remove Duplicates from Sorted List II", "Medium"], [83, "Remove Duplicates from Sorted List", "Easy"], [84, "Largest Rectangle in Histogram", "Hard"],
    [85, "Maximal Rectangle", "Hard"], [86, "Partition List", "Medium"], [87, "Scramble String", "Hard"],
    [88, "Merge Sorted Array", "Easy"], [89, "Gray Code", "Medium"], [90, "Subsets II", "Medium"],
    [91, "Decode Ways", "Medium"], [92, "Reverse Linked List II", "Medium"], [93, "Restore IP Addresses", "Medium"],
    [94, "Binary Tree Inorder Traversal", "Easy"], [95, "Unique Binary Search Trees II", "Medium"], [96, "Unique Binary Search Trees", "Medium"],
    [97, "Interleaving String", "Medium"], [98, "Validate Binary Search Tree", "Medium"], [99, "Recover Binary Search Tree", "Medium"],
    [100, "Same Tree", "Easy"]
];

const hardcoded = {
    1: { title: "Two Sum", difficulty: "Easy", time: "O(n)", space: "O(n)", code: `#include <vector>\n#include <unordered_map>\nusing namespace std;\n\nclass Solution {\npublic:\n    vector<int> twoSum(vector<int>& nums, int target) {\n        unordered_map<int,int> seen;\n        for(int i=0;i<(int)nums.size();i++){\n            int c=target-nums[i];\n            if(seen.count(c)) return{seen[c],i};\n            seen[nums[i]]=i;\n        }\n        return{};\n    }\n};` },
    2: { title: "Add Two Numbers", difficulty: "Medium", time: "O(max(m,n))", space: "O(max(m,n))", code: `struct ListNode{int val;ListNode*next;ListNode(int x):val(x),next(nullptr){}};\n\nclass Solution{\npublic:\n    ListNode*addTwoNumbers(ListNode*l1,ListNode*l2){\n        ListNode dummy(0);ListNode*cur=&dummy;int carry=0;\n        while(l1||l2||carry){\n            int sum=carry;\n            if(l1){sum+=l1->val;l1=l1->next;}\n            if(l2){sum+=l2->val;l2=l2->next;}\n            carry=sum/10;\n            cur->next=new ListNode(sum%10);\n            cur=cur->next;\n        }\n        return dummy.next;\n    }\n};` },
    3: { title: "Longest Substring Without Repeating Characters", difficulty: "Medium", time: "O(n)", space: "O(min(n,|Σ|))", code: `#include <unordered_map>\n#include <string>\nusing namespace std;\n\nclass Solution{\npublic:\n    int lengthOfLongestSubstring(string s){\n        unordered_map<char,int> last;\n        int maxLen=0,left=0;\n        for(int r=0;r<(int)s.size();r++){\n            if(last.count(s[r])&&last[s[r]]>=left) left=last[s[r]]+1;\n            last[s[r]]=r;\n            maxLen=max(maxLen,r-left+1);\n        }\n        return maxLen;\n    }\n};` },
    4: { title: "Median of Two Sorted Arrays", difficulty: "Hard", time: "O(log(min(m,n)))", space: "O(1)", code: `#include <vector>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2) {\n        if (nums1.size() > nums2.size()) return findMedianSortedArrays(nums2, nums1);\n        int m = nums1.size(), n = nums2.size();\n        int left = 0, right = m;\n        while (left <= right) {\n            int i = left + (right - left) / 2;\n            int j = (m + n + 1) / 2 - i;\n            int maxLeft1 = (i == 0) ? INT_MIN : nums1[i-1];\n            int minRight1 = (i == m) ? INT_MAX : nums1[i];\n            int maxLeft2 = (j == 0) ? INT_MIN : nums2[j-1];\n            int minRight2 = (j == n) ? INT_MAX : nums2[j];\n            if (maxLeft1 <= minRight2 && maxLeft2 <= minRight1) {\n                if ((m + n) % 2 == 0)\n                    return (max(maxLeft1, maxLeft2) + min(minRight1, minRight2)) / 2.0;\n                else\n                    return max(maxLeft1, maxLeft2);\n            } else if (maxLeft1 > minRight2) right = i - 1;\n            else left = i + 1;\n        }\n        return 0.0;\n    }\n};` },
    5: { title: "Longest Palindromic Substring", difficulty: "Medium", time: "O(n²)", space: "O(1)", code: `#include <string>\nusing namespace std;\n\nclass Solution {\npublic:\n    string longestPalindrome(string s) {\n        if (s.size() < 2) return s;\n        int start = 0, maxLen = 0;\n        auto expand = [&](int l, int r) {\n            while (l >= 0 && r < s.size() && s[l] == s[r]) { l--; r++; }\n            if (r - l - 1 > maxLen) { start = l + 1; maxLen = r - l - 1; }\n        };\n        for (int i = 0; i < s.size(); i++) {\n            expand(i, i);\n            expand(i, i + 1);\n        }\n        return s.substr(start, maxLen);\n    }\n};` },
    6: { title: "Zigzag Conversion", difficulty: "Medium", time: "O(n)", space: "O(n)", code: `#include <string>\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    string convert(string s, int numRows) {\n        if (numRows == 1) return s;\n        vector<string> rows(min(numRows, (int)s.size()));\n        int curRow = 0; bool goingDown = false;\n        for (char c : s) {\n            rows[curRow] += c;\n            if (curRow == 0 || curRow == numRows - 1) goingDown = !goingDown;\n            curRow += goingDown ? 1 : -1;\n        }\n        string res = "";\n        for (string row : rows) res += row;\n        return res;\n    }\n};` },
    7: { title: "Reverse Integer", difficulty: "Medium", time: "O(log x)", space: "O(1)", code: `#include <climits>\nusing namespace std;\n\nclass Solution{\npublic:\n    int reverse(int x){\n        int rev=0;\n        while(x!=0){\n            int d=x%10;x/=10;\n            if(rev>INT_MAX/10||(rev==INT_MAX/10&&d>7)) return 0;\n            if(rev<INT_MIN/10||(rev==INT_MIN/10&&d<-8)) return 0;\n            rev=rev*10+d;\n        }\n        return rev;\n    }\n};` },
    8: { title: "String to Integer (atoi)", difficulty: "Medium", time: "O(n)", space: "O(1)", code: `#include <string>\n#include <climits>\nusing namespace std;\n\nclass Solution {\npublic:\n    int myAtoi(string s) {\n        int i = 0, sign = 1, res = 0;\n        while (i < s.size() && s[i] == ' ') i++;\n        if (i < s.size() && (s[i] == '+' || s[i] == '-')) sign = (s[i++] == '-') ? -1 : 1;\n        while (i < s.size() && isdigit(s[i])) {\n            int d = s[i++] - '0';\n            if (res > INT_MAX / 10 || (res == INT_MAX / 10 && d > 7)) return sign == 1 ? INT_MAX : INT_MIN;\n            res = res * 10 + d;\n        }\n        return res * sign;\n    }\n};` },
    9: { title: "Palindrome Number", difficulty: "Easy", time: "O(log n)", space: "O(1)", code: `class Solution{\npublic:\n    bool isPalindrome(int x){\n        if(x<0||(x%10==0&&x!=0)) return false;\n        int rev=0;\n        while(x>rev){rev=rev*10+x%10;x/=10;}\n        return x==rev||x==rev/10;\n    }\n};` },
    10: { title: "Regular Expression Matching", difficulty: "Hard", time: "O(m·n)", space: "O(m·n)", code: `#include <string>\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    bool isMatch(string s, string p) {\n        int m = s.size(), n = p.size();\n        vector<vector<bool>> dp(m + 1, vector<bool>(n + 1, false));\n        dp[0][0] = true;\n        for (int j = 2; j <= n; j++) if (p[j-1] == '*') dp[0][j] = dp[0][j-2];\n        for (int i = 1; i <= m; i++) {\n            for (int j = 1; j <= n; j++) {\n                if (p[j-1] == '.' || s[i-1] == p[j-1]) dp[i][j] = dp[i-1][j-1];\n                else if (p[j-1] == '*') {\n                    dp[i][j] = dp[i][j-2];\n                    if (p[j-2] == '.' || s[i-1] == p[j-2]) dp[i][j] = dp[i][j] || dp[i-1][j];\n                }\n            }\n        }\n        return dp[m][n];\n    }\n};` },
    11: { title: "Container With Most Water", difficulty: "Medium", time: "O(n)", space: "O(1)", code: `#include <vector>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int maxArea(vector<int>& height) {\n        int l = 0, r = height.size() - 1, res = 0;\n        while (l < r) {\n            res = max(res, min(height[l], height[r]) * (r - l));\n            if (height[l] < height[r]) l++; else r--;\n        }\n        return res;\n    }\n};` },
    12: { title: "Integer to Roman", difficulty: "Medium", time: "O(1)", space: "O(1)", code: `#include <string>\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    string intToRoman(int num) {\n        vector<pair<int, string>> m = {{1000,"M"},{900,"CM"},{500,"D"},{400,"CD"},{100,"C"},{90,"XC"},{50,"L"},{40,"XL"},{10,"X"},{9,"IX"},{5,"V"},{4,"IV"},{1,"I"}};\n        string res = "";\n        for (auto& p : m) {\n            while (num >= p.first) { res += p.second; num -= p.first; }\n        }\n        return res;\n    }\n};` },
    13: { title: "Roman to Integer", difficulty: "Easy", time: "O(n)", space: "O(1)", code: `#include <string>\n#include <unordered_map>\nusing namespace std;\n\nclass Solution{\npublic:\n    int romanToInt(string s){\n        unordered_map<char,int> val={{'I',1},{'V',5},{'X',10},{'L',50},{'C',100},{'D',500},{'M',1000}};\n        int res=0;\n        for(int i=0;i<(int)s.size();i++)\n            res+=(i+1<(int)s.size()&&val[s[i]]<val[s[i+1]])?-val[s[i]]:val[s[i]];\n        return res;\n    }\n};` },
    14: { title: "Longest Common Prefix", difficulty: "Easy", time: "O(S)", space: "O(1)", code: `#include <string>\n#include <vector>\nusing namespace std;\n\nclass Solution{\npublic:\n    string longestCommonPrefix(vector<string>&strs){\n        if(strs.empty()) return"";\n        string prefix=strs[0];\n        for(int i=1;i<(int)strs.size();i++)\n            while(strs[i].find(prefix)!=0) prefix=prefix.substr(0,prefix.size()-1);\n        return prefix;\n    }\n};` },
    15: { title: "3Sum", difficulty: "Medium", time: "O(n²)", space: "O(1)", code: `#include <vector>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    vector<vector<int>> threeSum(vector<int>& nums) {\n        sort(nums.begin(), nums.end());\n        vector<vector<int>> res;\n        for (int i = 0; i < (int)nums.size() - 2; i++) {\n            if (i > 0 && nums[i] == nums[i-1]) continue;\n            int l = i + 1, r = nums.size() - 1;\n            while (l < r) {\n                int s = nums[i] + nums[l] + nums[r];\n                if (s < 0) l++;\n                else if (s > 0) r--;\n                else {\n                    res.push_back({nums[i], nums[l], nums[r]});\n                    while (l < r && nums[l] == nums[l+1]) l++;\n                    while (l < r && nums[r] == nums[r-1]) r--;\n                    l++; r--;\n                }\n            }\n        }\n        return res;\n    }\n};` },
    16: { title: "3Sum Closest", difficulty: "Medium", time: "O(n²)", space: "O(1)", code: `#include <vector>\n#include <algorithm>\n#include <climits>\nusing namespace std;\n\nclass Solution {\npublic:\n    int threeSumClosest(vector<int>& nums, int target) {\n        sort(nums.begin(), nums.end());\n        long res = INT_MAX;\n        for (int i = 0; i < (int)nums.size() - 2; i++) {\n            int l = i + 1, r = nums.size() - 1;\n            while (l < r) {\n                int s = nums[i] + nums[l] + nums[r];\n                if (abs(target - s) < abs(target - res)) res = s;\n                if (s < target) l++; else r--;\n            }\n        }\n        return (int)res;\n    }\n};` },
    17: { title: "Letter Combinations of a Phone Number", difficulty: "Medium", time: "O(4ⁿ)", space: "O(4ⁿ)", code: `#include <vector>\n#include <string>\nusing namespace std;\n\nclass Solution {\n    vector<string> m = {"","","abc","def","ghi","jkl","mno","pqrs","tuv","wxyz"};\n    void backtrack(vector<string>& res, string cur, string digits, int idx) {\n        if (idx == digits.size()) { res.push_back(cur); return; }\n        for (char c : m[digits[idx]-'0']) backtrack(res, cur + c, digits, idx + 1);\n    }\npublic:\n    vector<string> letterCombinations(string digits) {\n        if (digits.empty()) return {};\n        vector<string> res; backtrack(res, "", digits, 0); return res;\n    }\n};` },
    18: { title: "4Sum", difficulty: "Medium", time: "O(n³)", space: "O(1)", code: `#include <vector>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    vector<vector<int>> fourSum(vector<int>& nums, int target) {\n        sort(nums.begin(), nums.end());\n        vector<vector<int>> res;\n        int n = nums.size();\n        for (int i = 0; i < n - 3; i++) {\n            if (i > 0 && nums[i] == nums[i-1]) continue;\n            for (int j = i + 1; j < n - 2; j++) {\n                if (j > i + 1 && nums[j] == nums[j-1]) continue;\n                int l = j + 1, r = n - 1;\n                while (l < r) {\n                    long s = (long)nums[i] + nums[j] + nums[l] + nums[r];\n                    if (s < target) l++;\n                    else if (s > target) r--;\n                    else {\n                        res.push_back({nums[i], nums[j], nums[l], nums[r]});\n                        while (l < r && nums[l] == nums[l+1]) l++;\n                        while (l < r && nums[r] == nums[r-1]) r--;\n                        l++; r--;\n                    }\n                }\n            }\n        }\n        return res;\n    }\n};` },
    19: { title: "Remove Nth Node From End of List", difficulty: "Medium", time: "O(n)", space: "O(1)", code: `struct ListNode{int val;ListNode*next;ListNode(int x):val(x),next(nullptr){}};\n\nclass Solution {\npublic:\n    ListNode* removeNthFromEnd(ListNode* head, int n) {\n        ListNode dummy(0); dummy.next = head;\n        ListNode *f = &dummy, *s = &dummy;\n        for (int i = 0; i <= n; i++) f = f->next;\n        while (f) { f = f->next; s = s->next; }\n        s->next = s->next->next;\n        return dummy.next;\n    }\n};` },
    20: { title: "Valid Parentheses", difficulty: "Easy", time: "O(n)", space: "O(n)", code: `#include <stack>\n#include <string>\nusing namespace std;\n\nclass Solution{\npublic:\n    bool isValid(string s){\n        stack<char> st;\n        for(char c:s){\n            if(c=='('||c=='['||c=='{') st.push(c);\n            else{\n                if(st.empty()) return false;\n                char t=st.top();st.pop();\n                if((c==')'&&t!='(')||(c==']'&&t!='[')||(c=='}'&&t!='{')) return false;\n            }\n        }\n        return st.empty();\n    }\n};` },
    21: { title: "Merge Two Sorted Lists", difficulty: "Easy", time: "O(m+n)", space: "O(1)", code: `struct ListNode{int val;ListNode*next;ListNode(int x):val(x),next(nullptr){}};\n\nclass Solution{\npublic:\n    ListNode*mergeTwoLists(ListNode*l1,ListNode*l2){\n        ListNode dummy(0);ListNode*cur=&dummy;\n        while(l1&&l2){\n            if(l1->val<=l2->val){cur->next=l1;l1=l1->next;}\n            else{cur->next=l2;l2=l2->next;}\n            cur=cur->next;\n        }\n        cur->next=l1?l1:l2;\n        return dummy.next;\n    }\n};` },
    22: { title: "Generate Parentheses", difficulty: "Medium", time: "O(4ⁿ/√n)", space: "O(n)", code: `#include <vector>\n#include <string>\nusing namespace std;\n\nclass Solution {\n    void backtrack(vector<string>& res, string cur, int open, int close, int n) {\n        if (cur.size() == 2 * n) { res.push_back(cur); return; }\n        if (open < n) backtrack(res, cur + '(', open + 1, close, n);\n        if (close < open) backtrack(res, cur + ')', open, close + 1, n);\n    }\npublic:\n    vector<string> generateParenthesis(int n) {\n        vector<string> res; backtrack(res, "", 0, 0, n); return res;\n    }\n};` },
    23: { title: "Merge k Sorted Lists", difficulty: "Hard", time: "O(N log k)", space: "O(k)", code: `#include <queue>\nusing namespace std;\nstruct ListNode{int val;ListNode*next;ListNode(int x):val(x),next(nullptr){}};\n\nclass Solution {\npublic:\n    ListNode* mergeKLists(vector<ListNode*>& lists) {\n        auto cmp = [](ListNode* a, ListNode* b) { return a->val > b->val; };\n        priority_queue<ListNode*, vector<ListNode*>, decltype(cmp)> pq(cmp);\n        for (auto l : lists) if (l) pq.push(l);\n        ListNode dummy(0); ListNode* cur = &dummy;\n        while (!pq.empty()) {\n            ListNode* node = pq.top(); pq.pop();\n            cur->next = node; cur = cur->next;\n            if (node->next) pq.push(node->next);\n        }\n        return dummy.next;\n    }\n};` },
    24: { title: "Swap Nodes in Pairs", difficulty: "Medium", time: "O(n)", space: "O(1)", code: `struct ListNode{int val;ListNode*next;ListNode(int x):val(x),next(nullptr){}};\n\nclass Solution {\npublic:\n    ListNode* swapPairs(ListNode* head) {\n        if (!head || !head->next) return head;\n        ListNode* next = head->next;\n        head->next = swapPairs(next->next);\n        next->next = head;\n        return next;\n    }\n};` },
    25: { title: "Reverse Nodes in k-Group", difficulty: "Hard", time: "O(n)", space: "O(n/k)", code: `struct ListNode{int val;ListNode*next;ListNode(int x):val(x),next(nullptr){}};\n\nclass Solution {\npublic:\n    ListNode* reverseKGroup(ListNode* head, int k) {\n        ListNode* curr = head;\n        int count = 0;\n        while (curr && count != k) { curr = curr->next; count++; }\n        if (count == k) {\n            curr = reverseKGroup(curr, k);\n            while (count-- > 0) {\n                ListNode* tmp = head->next;\n                head->next = curr;\n                curr = head;\n                head = tmp;\n            }\n            head = curr;\n        }\n        return head;\n    }\n};` },
    26: { title: "Remove Duplicates from Sorted Array", difficulty: "Easy", time: "O(n)", space: "O(1)", code: `#include <vector>\nusing namespace std;\n\nclass Solution{\npublic:\n    int removeDuplicates(vector<int>&nums){\n        if(nums.empty()) return 0;\n        int k=1;\n        for(int i=1;i<(int)nums.size();i++)\n            if(nums[i]!=nums[i-1]) nums[k++]=nums[i];\n        return k;\n    }\n};` },
    27: { title: "Remove Element", difficulty: "Easy", time: "O(n)", space: "O(1)", code: `#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int removeElement(vector<int>& nums, int val) {\n        int k = 0;\n        for (int i = 0; i < nums.size(); i++) if (nums[i] != val) nums[k++] = nums[i];\n        return k;\n    }\n};` },
    28: { title: "Find the Index of the First Occurrence in a String", difficulty: "Easy", time: "O(m·n)", space: "O(1)", code: `#include <string>\nusing namespace std;\n\nclass Solution {\npublic:\n    int strStr(string haystack, string needle) {\n        int n = haystack.size(), m = needle.size();\n        for (int i = 0; i <= n - m; i++) if (haystack.substr(i, m) == needle) return i;\n        return -1;\n    }\n};` },
    29: { title: "Divide Two Integers", difficulty: "Medium", time: "O(log² n)", space: "O(1)", code: `#include <climits>\nusing namespace std;\n\nclass Solution{\npublic:\n    int divide(int dividend,int divisor){\n        if(dividend==INT_MIN&&divisor==-1) return INT_MAX;\n        long a=abs((long)dividend),b=abs((long)divisor);\n        int sign=(dividend>0)==(divisor>0)?1:-1;\n        long res=0;\n        while(a>=b){\n            long temp=b,mul=1;\n            while(a>=(temp<<1)){temp<<=1;mul<<=1;}\n            a-=temp;res+=mul;\n        }\n        return(int)(sign*res);\n    }\n};` },
    30: { title: "Substring with Concatenation of All Words", difficulty: "Hard", time: "O(n·k·l)", space: "O(k·l)", code: `#include <vector>\n#include <string>\n#include <unordered_map>\nusing namespace std;\n\nclass Solution {\npublic:\n    vector<int> findSubstring(string s, vector<string>& words) {\n        if (s.empty() || words.empty()) return {};\n        int n = s.size(), k = words.size(), l = words[0].size();\n        unordered_map<string, int> counts;\n        for (string w : words) counts[w]++;\n        vector<int> res;\n        for (int i = 0; i <= n - k * l; i++) {\n            unordered_map<string, int> seen;\n            int j = 0;\n            while (j < k) {\n                string word = s.substr(i + j * l, l);\n                if (counts.count(word)) {\n                    seen[word]++;\n                    if (seen[word] > counts[word]) break;\n                } else break;\n                j++;\n            }\n            if (j == k) res.push_back(i);\n        }\n        return res;\n    }\n};` },
    31: { title: "Next Permutation", difficulty: "Medium", time: "O(n)", space: "O(1)", code: `#include <vector>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    void nextPermutation(vector<int>& nums) {\n        int n = nums.size(), k, l;\n        for (k = n - 2; k >= 0; k--) if (nums[k] < nums[k + 1]) break;\n        if (k < 0) reverse(nums.begin(), nums.end());\n        else {\n            for (l = n - 1; l > k; l--) if (nums[l] > nums[k]) break;\n            swap(nums[k], nums[l]);\n            reverse(nums.begin() + k + 1, nums.end());\n        }\n    }\n};` },
    32: { title: "Longest Valid Parentheses", difficulty: "Hard", time: "O(n)", space: "O(n)", code: `#include <string>\n#include <stack>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int longestValidParentheses(string s) {\n        stack<int> st; st.push(-1);\n        int res = 0;\n        for (int i = 0; i < s.size(); i++) {\n            if (s[i] == '(') st.push(i);\n            else {\n                st.pop();\n                if (st.empty()) st.push(i);\n                else res = max(res, i - st.top());\n            }\n        }\n        return res;\n    }\n};` },
    33: { title: "Search in Rotated Sorted Array", difficulty: "Medium", time: "O(log n)", space: "O(1)", code: `#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int search(vector<int>& nums, int target) {\n        int l = 0, r = nums.size() - 1;\n        while (l <= r) {\n            int m = l + (r - l) / 2;\n            if (nums[m] == target) return m;\n            if (nums[l] <= nums[m]) {\n                if (nums[l] <= target && target < nums[m]) r = m - 1; else l = m + 1;\n            } else {\n                if (nums[m] < target && target <= nums[r]) l = m + 1; else r = m - 1;\n            }\n        }\n        return -1;\n    }\n};` },
    34: { title: "Find First and Last Position of Element in Sorted Array", difficulty: "Medium", time: "O(log n)", space: "O(1)", code: `#include <vector>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    vector<int> searchRange(vector<int>& nums, int target) {\n        auto it1 = lower_bound(nums.begin(), nums.end(), target);\n        auto it2 = upper_bound(nums.begin(), nums.end(), target);\n        if (it1 == nums.end() || *it1 != target) return {-1, -1};\n        return {(int)(it1 - nums.begin()), (int)(it2 - nums.begin() - 1)};\n    }\n};` },
    35: { title: "Search Insert Position", difficulty: "Easy", time: "O(log n)", space: "O(1)", code: `#include <vector>\nusing namespace std;\n\nclass Solution{\npublic:\n    int searchInsert(vector<int>&nums,int target){\n        int l=0,r=(int)nums.size()-1;\n        while(l<=r){\n            int m=l+(r-l)/2;\n            if(nums[m]==target) return m;\n            else if(nums[m]<target) l=m+1;\n            else r=m-1;\n        }\n        return l;\n    }\n};` },
    36: { title: "Valid Sudoku", difficulty: "Medium", time: "O(1)", space: "O(1)", code: `#include <vector>\n#include <unordered_set>\n#include <string>\nusing namespace std;\n\nclass Solution {\npublic:\n    bool isValidSudoku(vector<vector<char>>& board) {\n        unordered_set<string> seen;\n        for (int i=0; i<9; i++) {\n            for (int j=0; j<9; j++) {\n                if (board[i][j] != '.') {\n                    string b = "(" + to_string(board[i][j]) + ")";\n                    if (!seen.insert(b + to_string(i)).second ||\n                        !seen.insert(to_string(j) + b).second ||\n                        !seen.insert(to_string(i/3) + b + to_string(j/3)).second) return false;\n                }\n            }\n        }\n        return true;\n    }\n};` },
    37: { title: "Sudoku Solver", difficulty: "Hard", time: "O(9^m)", space: "O(1)", code: `#include <vector>\nusing namespace std;\n\nclass Solution {\n    bool solve(vector<vector<char>>& board) {\n        for (int i = 0; i < 9; i++) {\n            for (int j = 0; j < 9; j++) {\n                if (board[i][j] == '.') {\n                    for (char c = '1'; c <= '9'; c++) {\n                        if (isValid(board, i, j, c)) {\n                            board[i][j] = c;\n                            if (solve(board)) return true;\n                            board[i][j] = '.';\n                        }\n                    }\n                    return false;\n                }\n            }\n        }\n        return true;\n    }\n    bool isValid(vector<vector<char>>& b, int r, int c, char v) {\n        for (int i = 0; i < 9; i++) {\n            if (b[i][c] == v) return false;\n            if (b[r][i] == v) return false;\n            if (b[3 * (r / 3) + i / 3][3 * (c / 3) + i % 3] == v) return false;\n        }\n        return true;\n    }\npublic:\n    void solveSudoku(vector<vector<char>>& board) { solve(board); }\n};` },
    38: { title: "Count and Say", difficulty: "Medium", time: "O(2ⁿ)", space: "O(2ⁿ)", code: `#include <string>\nusing namespace std;\n\nclass Solution {\npublic:\n    string countAndSay(int n) {\n        if (n == 1) return "1";\n        string s = countAndSay(n - 1), res = "";\n        for (int i = 0; i < s.size(); i++) {\n            int count = 1;\n            while (i + 1 < s.size() && s[i] == s[i + 1]) { i++; count++; }\n            res += to_string(count) + s[i];\n        }\n        return res;\n    }\n};` },
    39: { title: "Combination Sum", difficulty: "Medium", time: "O(2ⁿ)", space: "O(target/min)", code: `#include <vector>\nusing namespace std;\n\nclass Solution {\n    void backtrack(vector<int>& c, int t, int s, vector<int>& cur, vector<vector<int>>& res) {\n        if (t == 0) { res.push_back(cur); return; }\n        for (int i = s; i < c.size(); i++) {\n            if (c[i] <= t) {\n                cur.push_back(c[i]);\n                backtrack(c, t - c[i], i, cur, res);\n                cur.pop_back();\n            }\n        }\n    }\npublic:\n    vector<vector<int>> combinationSum(vector<int>& candidates, int target) {\n        vector<vector<int>> res; vector<int> cur; backtrack(candidates, target, 0, cur, res); return res;\n    }\n};` },
    40: { title: "Combination Sum II", difficulty: "Medium", time: "O(2ⁿ)", space: "O(n)", code: `#include <vector>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\n    void backtrack(vector<int>& c, int t, int s, vector<int>& cur, vector<vector<int>>& res) {\n        if (t == 0) { res.push_back(cur); return; }\n        for (int i = s; i < c.size(); i++) {\n            if (i > s && c[i] == c[i-1]) continue;\n            if (c[i] > t) break;\n            cur.push_back(c[i]);\n            backtrack(c, t - c[i], i + 1, cur, res);\n            cur.pop_back();\n        }\n    }\npublic:\n    vector<vector<int>> combinationSum2(vector<int>& candidates, int target) {\n        sort(candidates.begin(), candidates.end());\n        vector<vector<int>> res; vector<int> cur; backtrack(candidates, target, 0, cur, res); return res;\n    }\n};` },
    41: { title: "First Missing Positive", difficulty: "Hard", time: "O(n)", space: "O(1)", code: `#include <vector>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int firstMissingPositive(vector<int>& nums) {\n        int n = nums.size();\n        for (int i = 0; i < n; i++)\n            while (nums[i] > 0 && nums[i] <= n && nums[nums[i]-1] != nums[i]) swap(nums[i], nums[nums[i]-1]);\n        for (int i = 0; i < n; i++) if (nums[i] != i + 1) return i + 1;\n        return n + 1;\n    }\n};` },
    42: { title: "Trapping Rain Water", difficulty: "Hard", time: "O(n)", space: "O(1)", code: `#include <vector>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int trap(vector<int>& height) {\n        int l = 0, r = height.size() - 1, lMax = 0, rMax = 0, res = 0;\n        while (l < r) {\n            if (height[l] < height[r]) {\n                if (height[l] >= lMax) lMax = height[l]; else res += lMax - height[l];\n                l++;\n            } else {\n                if (height[r] >= rMax) rMax = height[r]; else res += rMax - height[r];\n                r--;\n            }\n        }\n        return res;\n    }\n};` },
    43: { title: "Multiply Strings", difficulty: "Medium", time: "O(m·n)", space: "O(m+n)", code: `#include <string>\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    string multiply(string num1, string num2) {\n        if (num1 == "0" || num2 == "0") return "0";\n        vector<int> res(num1.size() + num2.size(), 0);\n        for (int i = num1.size() - 1; i >= 0; i--) {\n            for (int j = num2.size() - 1; j >= 0; j--) {\n                res[i + j + 1] += (num1[i] - '0') * (num2[j] - '0');\n                res[i + j] += res[i + j + 1] / 10;\n                res[i + j + 1] %= 10;\n            }\n        }\n        int i = 0; while (i < res.size() && res[i] == 0) i++;\n        string s = ""; while (i < res.size()) s += to_string(res[i++]);\n        return s;\n    }\n};` },
    44: { title: "Wildcard Matching", difficulty: "Hard", time: "O(m·n)", space: "O(1)", code: `#include <string>\nusing namespace std;\n\nclass Solution {\npublic:\n    bool isMatch(string s, string p) {\n        int i = 0, j = 0, star = -1, match = 0;\n        while (i < s.size()) {\n            if (j < p.size() && (p[j] == '?' || s[i] == p[j])) { i++; j++; }\n            else if (j < p.size() && p[j] == '*') { star = j++; match = i; }\n            else if (star != -1) { j = star + 1; i = ++match; }\n            else return false;\n        }\n        while (j < p.size() && p[j] == '*') j++;\n        return j == p.size();\n    }\n};` },
    45: { title: "Jump Game II", difficulty: "Medium", time: "O(n)", space: "O(1)", code: `#include <vector>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int jump(vector<int>& nums) {\n        int jumps = 0, curEnd = 0, curFarthest = 0;\n        for (int i = 0; i < (int)nums.size() - 1; i++) {\n            curFarthest = max(curFarthest, i + nums[i]);\n            if (i == curEnd) { jumps++; curEnd = curFarthest; }\n        }\n        return jumps;\n    }\n};` },
    46: { title: "Permutations", difficulty: "Medium", time: "O(n·n!)", space: "O(n!)", code: `#include <vector>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\n    void backtrack(vector<int>& nums, int start, vector<vector<int>>& res) {\n        if (start == nums.size()) { res.push_back(nums); return; }\n        for (int i = start; i < nums.size(); i++) {\n            swap(nums[start], nums[i]);\n            backtrack(nums, start + 1, res);\n            swap(nums[start], nums[i]);\n        }\n    }\npublic:\n    vector<vector<int>> permute(vector<int>& nums) {\n        vector<vector<int>> res; backtrack(nums, 0, res); return res;\n    }\n};` },
    47: { title: "Permutations II", difficulty: "Medium", time: "O(n·n!)", space: "O(n)", code: `#include <vector>\n#include <algorithm>\n#include <unordered_set>\nusing namespace std;\n\nclass Solution {\n    void backtrack(vector<int> nums, int start, vector<vector<int>>& res) {\n        if (start == nums.size()) { res.push_back(nums); return; }\n        for (int i = start; i < nums.size(); i++) {\n            if (i > start && nums[i] == nums[start]) continue;\n            swap(nums[start], nums[i]);\n            backtrack(nums, start + 1, res);\n        }\n    }\npublic:\n    vector<vector<int>> permuteUnique(vector<int>& nums) {\n        sort(nums.begin(), nums.end());\n        vector<vector<int>> res; backtrack(nums, 0, res); return res;\n    }\n};` },
    48: { title: "Rotate Image", difficulty: "Medium", time: "O(n²)", space: "O(1)", code: `#include <vector>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    void rotate(vector<vector<int>>& matrix) {\n        int n = matrix.size();\n        for (int i = 0; i < n; i++)\n            for (int j = i; j < n; j++) swap(matrix[i][j], matrix[j][i]);\n        for (int i = 0; i < n; i++) reverse(matrix[i].begin(), matrix[i].end());\n    }\n};` },
    49: { title: "Group Anagrams", difficulty: "Medium", time: "O(n·k log k)", space: "O(n·k)", code: `#include <vector>\n#include <string>\n#include <unordered_map>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    vector<vector<string>> groupAnagrams(vector<string>& strs) {\n        unordered_map<string, vector<string>> groups;\n        for (string s : strs) {\n            string key = s; sort(key.begin(), key.end());\n            groups[key].push_back(s);\n        }\n        vector<vector<string>> res;\n        for (auto& p : groups) res.push_back(p.second);\n        return res;\n    }\n};` },
    50: { title: "Pow(x, n)", difficulty: "Medium", time: "O(log n)", space: "O(log n)", code: `class Solution {\npublic:\n    double myPow(double x, int n) {\n        if (n == 0) return 1;\n        if (n < 0) { x = 1 / x; n = -(n + 1); return x * myPow(x, n); }\n        return (n % 2 == 0) ? myPow(x * x, n / 2) : x * myPow(x * x, n / 2);\n    }\n};` },
    51: { title: "N-Queens", difficulty: "Hard", time: "O(n!)", space: "O(n)", code: `#include <vector>\n#include <string>\nusing namespace std;\n\nclass Solution{\n    vector<vector<string>> res;\n    void solve(int row,int n,vector<string>&board,vector<bool>&col,vector<bool>&d1,vector<bool>&d2){\n        if(row==n){res.push_back(board);return;}\n        for(int c=0;c<n;c++){\n            if(col[c]||d1[row-c+n]||d2[row+c]) continue;\n            board[row][c]='Q';\n            col[c]=d1[row-c+n]=d2[row+c]=true;\n            solve(row+1,n,board,col,d1,d2);\n            board[row][c]='.';\n            col[c]=d1[row-c+n]=d2[row+c]=false;\n        }\n    }\npublic:\n    vector<vector<string>> solveNQueens(int n){\n        vector<string> board(n,string(n,'.'));\n        vector<bool> col(n,false),d1(2*n,false),d2(2*n,false);\n        solve(0,n,board,col,d1,d2);\n        return res;\n    }\n};` },
    52: { title: "N-Queens II", difficulty: "Hard", time: "O(n!)", space: "O(n)", code: `#include <vector>\nusing namespace std;\n\nclass Solution {\n    int count = 0;\n    void solve(int row, int n, int col, int d1, int d2) {\n        if (row == n) { count++; return; }\n        int available = ((1 << n) - 1) & ~(col | d1 | d2);\n        while (available) {\n            int p = available & -available;\n            available ^= p;\n            solve(row + 1, n, col | p, (d1 | p) << 1, (d2 | p) >> 1);\n        }\n    }\npublic:\n    int totalNQueens(int n) {\n        solve(0, n, 0, 0, 0); return count;\n    }\n};` },
    53: { title: "Maximum Subarray", difficulty: "Medium", time: "O(n)", space: "O(1)", code: `#include <vector>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int maxSubArray(vector<int>& nums) {\n        int res = nums[0], cur = nums[0];\n        for (int i = 1; i < nums.size(); i++) { cur = max(nums[i], cur + nums[i]); res = max(res, cur); }\n        return res;\n    }\n};` },
    54: { title: "Spiral Matrix", difficulty: "Medium", time: "O(m·n)", space: "O(1)", code: `#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    vector<int> spiralOrder(vector<vector<int>>& matrix) {\n        if (matrix.empty()) return {};\n        int r1 = 0, r2 = matrix.size()-1, c1 = 0, c2 = matrix[0].size()-1;\n        vector<int> res;\n        while (r1 <= r2 && c1 <= c2) {\n            for (int c = c1; c <= c2; c++) res.push_back(matrix[r1][c]);\n            for (int r = r1 + 1; r <= r2; r++) res.push_back(matrix[r][c2]);\n            if (r1 < r2 && c1 < c2) {\n                for (int c = c2 - 1; c > c1; c--) res.push_back(matrix[r2][c]);\n                for (int r = r2; r > r1; r--) res.push_back(matrix[r][c1]);\n            }\n            r1++; r2--; c1++; c2--;\n        }\n        return res;\n    }\n};` },
    55: { title: "Jump Game", difficulty: "Medium", time: "O(n)", space: "O(1)", code: `#include <vector>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    bool canJump(vector<int>& nums) {\n        int reach = 0;\n        for (int i = 0; i < nums.size(); i++) {\n            if (i > reach) return false;\n            reach = max(reach, i + nums[i]);\n        }\n        return true;\n    }\n};` },
    56: { title: "Merge Intervals", difficulty: "Medium", time: "O(n log n)", space: "O(n)", code: `#include <vector>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    vector<vector<int>> merge(vector<vector<int>>& intervals) {\n        if (intervals.empty()) return {};\n        sort(intervals.begin(), intervals.end());\n        vector<vector<int>> res = {intervals[0]};\n        for (int i = 1; i < intervals.size(); i++) {\n            if (intervals[i][0] <= res.back()[1]) res.back()[1] = max(res.back()[1], intervals[i][1]);\n            else res.push_back(intervals[i]);\n        }\n        return res;\n    }\n};` },
    57: { title: "Insert Interval", difficulty: "Medium", time: "O(n)", space: "O(n)", code: `#include <vector>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    vector<vector<int>> insert(vector<vector<int>>& intervals, vector<int>& newInterval) {\n        vector<vector<int>> res;\n        int i = 0, n = intervals.size();\n        while (i < n && intervals[i][1] < newInterval[0]) res.push_back(intervals[i++]);\n        while (i < n && intervals[i][0] <= newInterval[1]) {\n            newInterval[0] = min(newInterval[0], intervals[i][0]);\n            newInterval[1] = max(newInterval[1], intervals[i][1]);\n            i++;\n        }\n        res.push_back(newInterval);\n        while (i < n) res.push_back(intervals[i++]);\n        return res;\n    }\n};` },
    58: { title: "Length of Last Word", difficulty: "Easy", time: "O(n)", space: "O(1)", code: `#include <string>\nusing namespace std;\n\nclass Solution {\npublic:\n    int lengthOfLastWord(string s) {\n        int i = s.size() - 1, len = 0;\n        while (i >= 0 && s[i] == ' ') i--;\n        while (i >= 0 && s[i] != ' ') { len++; i--; }\n        return len;\n    }\n};` },
    59: { title: "Spiral Matrix II", difficulty: "Medium", time: "O(n²)", space: "O(1)", code: `#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    vector<vector<int>> generateMatrix(int n) {\n        vector<vector<int>> res(n, vector<int>(n));\n        int r1 = 0, r2 = n - 1, c1 = 0, c2 = n - 1, val = 1;\n        while (r1 <= r2 && c1 <= c2) {\n            for (int c = c1; c <= c2; c++) res[r1][c] = val++;\n            for (int r = r1 + 1; r <= r2; r++) res[r][c2] = val++;\n            if (r1 < r2 && c1 < c2) {\n                for (int c = c2 - 1; c > c1; c--) res[r2][c] = val++;\n                for (int r = r2; r > r1; r--) res[r][c1] = val++;\n            }\n            r1++; r2--; c1++; c2--;\n        }\n        return res;\n    }\n};` },
    60: { title: "Permutation Sequence", difficulty: "Hard", time: "O(n²)", space: "O(n)", code: `#include <vector>\n#include <string>\n#include <numeric>\nusing namespace std;\n\nclass Solution {\npublic:\n    string getPermutation(int n, int k) {\n        vector<int> f(n, 1), nums(n);\n        iota(nums.begin(), nums.end(), 1);\n        for (int i = 1; i < n; i++) f[i] = f[i - 1] * i;\n        k--; string res = "";\n        for (int i = n - 1; i >= 0; i--) {\n            int idx = k / f[i];\n            k %= f[i];\n            res += to_string(nums[idx]);\n            nums.erase(nums.begin() + idx);\n        }\n        return res;\n    }\n};` },
    61: { title: "Rotate List", difficulty: "Medium", time: "O(n)", space: "O(1)", code: `struct ListNode{int val;ListNode*next;ListNode(int x):val(x),next(nullptr){}};\n\nclass Solution {\npublic:\n    ListNode* rotateRight(ListNode* head, int k) {\n        if (!head || !head->next || k == 0) return head;\n        ListNode* cur = head; int len = 1;\n        while (cur->next) { cur = cur->next; len++; }\n        cur->next = head;\n        k %= len;\n        for (int i = 0; i < len - k; i++) cur = cur->next;\n        head = cur->next; cur->next = nullptr;\n        return head;\n    }\n};` },
    62: { title: "Unique Paths", difficulty: "Medium", time: "O(m·n)", space: "O(n)", code: `#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int uniquePaths(int m, int n) {\n        vector<int> dp(n, 1);\n        for (int i = 1; i < m; i++)\n            for (int j = 1; j < n; j++) dp[j] += dp[j-1];\n        return dp[n-1];\n    }\n};` },
    63: { title: "Unique Paths II", difficulty: "Medium", time: "O(m·n)", space: "O(n)", code: `#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int uniquePathsWithObstacles(vector<vector<int>>& og) {\n        int m = og.size(), n = og[0].size();\n        vector<long> dp(n, 0); dp[0] = 1;\n        for (int i = 0; i < m; i++) {\n            for (int j = 0; j < n; j++) {\n                if (og[i][j]) dp[j] = 0; else if (j > 0) dp[j] += dp[j-1];\n            }\n        }\n        return (int)dp[n-1];\n    }\n};` },
    64: { title: "Minimum Path Sum", difficulty: "Medium", time: "O(m·n)", space: "O(n)", code: `#include <vector>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int minPathSum(vector<vector<int>>& grid) {\n        int m = grid.size(), n = grid[0].size();\n        vector<int> dp(n, grid[0][0]);\n        for (int j = 1; j < n; j++) dp[j] = dp[j-1] + grid[0][j];\n        for (int i = 1; i < m; i++) {\n            dp[0] += grid[i][0];\n            for (int j = 1; j < n; j++) dp[j] = grid[i][j] + min(dp[j], dp[j-1]);\n        }\n        return dp[n-1];\n    }\n};` },
    65: { title: "Valid Number", difficulty: "Hard", time: "O(n)", space: "O(1)", code: `#include <string>\nusing namespace std;\n\nclass Solution {\npublic:\n    bool isNumber(string s) {\n        bool num = false, exp = false, dot = false;\n        for (int i = 0; i < s.size(); i++) {\n            if (isdigit(s[i])) num = true;\n            else if (s[i] == 'e' || s[i] == 'E') {\n                if (exp || !num) return false;\n                exp = true; num = false;\n            } else if (s[i] == '+' || s[i] == '-') {\n                if (i > 0 && s[i-1] != 'e' && s[i-1] != 'E') return false;\n            } else if (s[i] == '.') {\n                if (dot || exp) return false;\n                dot = true;\n            } else return false;\n        }\n        return num;\n    }\n};` },
    66: { title: "Plus One", difficulty: "Easy", time: "O(n)", space: "O(1)", code: `#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    vector<int> plusOne(vector<int>& digits) {\n        for (int i = digits.size() - 1; i >= 0; i--) {\n            if (digits[i] < 9) { digits[i]++; return digits; }\n            digits[i] = 0;\n        }\n        digits.insert(digits.begin(), 1); return digits;\n    }\n};` },
    67: { title: "Add Binary", difficulty: "Easy", time: "O(max(m,n))", space: "O(max(m,n))", code: `#include <string>\n#include <algorithm>\nusing namespace std;\n\nclass Solution{\npublic:\n    string addBinary(string a,string b){\n        string res="";\n        int i=(int)a.size()-1,j=(int)b.size()-1,carry=0;\n        while(i>=0||j>=0||carry){\n            int sum=carry;\n            if(i>=0) sum+=a[i--]-'0';\n            if(j>=0) sum+=b[j--]-'0';\n            res+=(char)('0'+sum%2);\n            carry=sum/2;\n        }\n        reverse(res.begin(),res.end());\n        return res;\n    }\n};` },
    68: { title: "Text Justification", difficulty: "Hard", time: "O(n)", space: "O(n)", code: `#include <vector>\n#include <string>\nusing namespace std;\n\nclass Solution {\npublic:\n    vector<string> fullJustify(vector<string>& words, int maxWidth) {\n        vector<string> res; int i = 0;\n        while (i < words.size()) {\n            int j = i + 1, len = words[i].size();\n            while (j < words.size() && len + words[j].size() + (j - i) <= maxWidth) { len += words[j].size(); j++; }\n            string line = words[i];\n            int spaces = maxWidth - len, gaps = j - i - 1;\n            if (j == words.size() || gaps == 0) {\n                for (int k = i + 1; k < j; k++) line += " " + words[k];\n                line += string(maxWidth - line.size(), ' ');\n            } else {\n                int q = spaces / gaps, r = spaces % gaps;\n                for (int k = i + 1; k < j; k++) line += string(q + (k - i <= r ? 1 : 0), ' ') + words[k];\n            }\n            res.push_back(line); i = j;\n        }\n        return res;\n    }\n};` },
    69: { title: "Sqrt(x)", difficulty: "Easy", time: "O(log x)", space: "O(1)", code: `class Solution {\npublic:\n    int mySqrt(int x) {\n        if (x < 2) return x;\n        long l = 1, r = x / 2;\n        while (l <= r) {\n            long m = l + (r - l) / 2;\n            if (m * m == x) return m;\n            if (m * m < x) l = m + 1; else r = m - 1;\n        }\n        return r;\n    }\n};` },
    70: { title: "Climbing Stairs", difficulty: "Easy", time: "O(n)", space: "O(1)", code: `class Solution{\npublic:\n    int climbStairs(int n){\n        if(n<=2) return n;\n        int a=1,b=2;\n        for(int i=3;i<=n;i++){int c=a+b;a=b;b=c;}\n        return b;\n    }\n};` },
    71: { title: "Simplify Path", difficulty: "Medium", time: "O(n)", space: "O(n)", code: `#include <string>\n#include <vector>\n#include <sstream>\nusing namespace std;\n\nclass Solution {\npublic:\n    string simplifyPath(string path) {\n        stringstream ss(path); string res, cur; vector<string> v;\n        while (getline(ss, cur, '/')) {\n            if (cur == "" || cur == ".") continue;\n            if (cur == "..") { if (!v.empty()) v.pop_back(); }\n            else v.push_back(cur);\n        }\n        for (string s : v) res += "/" + s;\n        return v.empty() ? "/" : res;\n    }\n};` },
    72: { title: "Edit Distance", difficulty: "Medium", time: "O(m·n)", space: "O(m·n)", code: `#include <string>\n#include <vector>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int minDistance(string w1, string w2) {\n        int m = w1.size(), n = w2.size();\n        vector<vector<int>> dp(m + 1, vector<int>(n + 1));\n        for (int i = 0; i <= m; i++) dp[i][0] = i;\n        for (int j = 0; j <= n; j++) dp[0][j] = j;\n        for (int i = 1; i <= m; i++) {\n            for (int j = 1; j <= n; j++) {\n                if (w1[i-1] == w2[j-1]) dp[i][j] = dp[i-1][j-1];\n                else dp[i][j] = 1 + min({dp[i-1][j], dp[i][j-1], dp[i-1][j-1]});\n            }\n        }\n        return dp[m][n];\n    }\n};` },
    73: { title: "Set Matrix Zeroes", difficulty: "Medium", time: "O(m·n)", space: "O(1)", code: `#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    void setZeroes(vector<vector<int>>& matrix) {\n        int m = matrix.size(), n = matrix[0].size();\n        bool r0 = false, c0 = false;\n        for (int i = 0; i < m; i++) if (matrix[i][0] == 0) c0 = true;\n        for (int j = 0; j < n; j++) if (matrix[0][j] == 0) r0 = true;\n        for (int i = 1; i < m; i++) for (int j = 1; j < n; j++) if (matrix[i][j] == 0) matrix[i][0] = matrix[0][j] = 0;\n        for (int i = 1; i < m; i++) for (int j = 1; j < n; j++) if (matrix[i][0] == 0 || matrix[0][j] == 0) matrix[i][j] = 0;\n        if (c0) for (int i = 0; i < m; i++) matrix[i][0] = 0;\n        if (r0) for (int j = 0; j < n; j++) matrix[0][j] = 0;\n    }\n};` },
    74: { title: "Search a 2D Matrix", difficulty: "Medium", time: "O(log(m·n))", space: "O(1)", code: `#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    bool searchMatrix(vector<vector<int>>& matrix, int target) {\n        int m = matrix.size(), n = matrix[0].size();\n        int l = 0, r = m * n - 1;\n        while (l <= r) {\n            int mid = l + (r - l) / 2;\n            int v = matrix[mid / n][mid % n];\n            if (v == target) return true;\n            if (v < target) l = mid + 1; else r = mid - 1;\n        }\n        return false;\n    }\n};` },
    75: { title: "Sort Colors", difficulty: "Medium", time: "O(n)", space: "O(1)", code: `#include <vector>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    void sortColors(vector<int>& nums) {\n        int low = 0, mid = 0, high = nums.size() - 1;\n        while (mid <= high) {\n            if (nums[mid] == 0) swap(nums[low++], nums[mid++]);\n            else if (nums[mid] == 1) mid++;\n            else swap(nums[mid], nums[high--]);\n        }\n    }\n};` },
    76: { title: "Minimum Window Substring", difficulty: "Hard", time: "O(n)", space: "O(1)", code: `#include <string>\n#include <vector>\n#include <climits>\nusing namespace std;\n\nclass Solution {\npublic:\n    string minWindow(string s, string t) {\n        vector<int> map(128, 0);\n        for (char c : t) map[c]++;\n        int counter = t.size(), begin = 0, end = 0, d = INT_MAX, head = 0;\n        while (end < s.size()) {\n            if (map[s[end++]]-- > 0) counter--;\n            while (counter == 0) {\n                if (end - begin < d) d = end - (head = begin);\n                if (map[s[begin++]]++ == 0) counter++;\n            }\n        }\n        return d == INT_MAX ? "" : s.substr(head, d);\n    }\n};` },
    77: { title: "Combinations", difficulty: "Medium", time: "O(nᵏ)", space: "O(k)", code: `#include <vector>\nusing namespace std;\n\nclass Solution {\n    void solve(int s, int n, int k, vector<int>& cur, vector<vector<int>>& res) {\n        if (cur.size() == k) { res.push_back(cur); return; }\n        for (int i = s; i <= n; i++) {\n            cur.push_back(i); solve(i + 1, n, k, cur, res); cur.pop_back();\n        }\n    }\npublic:\n    vector<vector<int>> combine(int n, int k) {\n        vector<vector<int>> res; vector<int> cur; solve(1, n, k, cur, res); return res;\n    }\n};` },
    78: { title: "Subsets", difficulty: "Medium", time: "O(n·2ⁿ)", space: "O(n·2ⁿ)", code: `#include <vector>\nusing namespace std;\n\nclass Solution{\npublic:\n    vector<vector<int>> subsets(vector<int>&nums){\n        int n=(int)nums.size();\n        vector<vector<int>> res;\n        for(int mask=0;mask<(1<<n);mask++){\n            vector<int> sub;\n            for(int i=0;i<n;i++) if(mask&(1<<i)) sub.push_back(nums[i]);\n            res.push_back(sub);\n        }\n        return res;\n    }\n};` },
    79: { title: "Word Search", difficulty: "Medium", time: "O(n·3ᵏ)", space: "O(k)", code: `#include <vector>\n#include <string>\nusing namespace std;\n\nclass Solution {\n    bool dfs(vector<vector<char>>& board, string& word, int i, int r, int c) {\n        if (i == word.size()) return true;\n        if (r < 0 || r >= board.size() || c < 0 || c >= board[0].size() || board[r][c] != word[i]) return false;\n        char tmp = board[r][c]; board[r][c] = '*';\n        bool res = dfs(board, word, i + 1, r + 1, c) || dfs(board, word, i + 1, r - 1, c) || dfs(board, word, i + 1, r, c + 1) || dfs(board, word, i + 1, r, c - 1);\n        board[r][c] = tmp; return res;\n    }\npublic:\n    bool exist(vector<vector<char>>& board, string word) {\n        for (int r = 0; r < board.size(); r++)\n            for (int c = 0; c < board[0].size(); c++) if (dfs(board, word, 0, r, c)) return true;\n        return false;\n    }\n};` },
    80: { title: "Remove Duplicates from Sorted Array II", difficulty: "Medium", time: "O(n)", space: "O(1)", code: `#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int removeDuplicates(vector<int>& nums) {\n        int i = 0; for (int n : nums) if (i < 2 || n > nums[i-2]) nums[i++] = n;\n        return i;\n    }\n};` },
    81: { title: "Search in Rotated Sorted Array II", difficulty: "Medium", time: "O(n)", space: "O(1)", code: `#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    bool search(vector<int>& nums, int target) {\n        int l = 0, r = nums.size() - 1;\n        while (l <= r) {\n            int m = l + (r - l) / 2;\n            if (nums[m] == target) return true;\n            if (nums[l] == nums[m] && nums[m] == nums[r]) { l++; r--; }\n            else if (nums[l] <= nums[m]) {\n                if (nums[l] <= target && target < nums[m]) r = m - 1; else l = m + 1;\n            } else {\n                if (nums[m] < target && target <= nums[r]) l = m + 1; else r = m - 1;\n            }\n        }\n        return false;\n    }\n};` },
    82: { title: "Remove Duplicates from Sorted List II", difficulty: "Medium", time: "O(n)", space: "O(1)", code: `struct ListNode{int val;ListNode*next;ListNode(int x):val(x),next(nullptr){}};\n\nclass Solution {\npublic:\n    ListNode* deleteDuplicates(ListNode* head) {\n        ListNode dummy(0); dummy.next = head; ListNode* prev = &dummy;\n        while (head) {\n            if (head->next && head->val == head->next->val) {\n                while (head->next && head->val == head->next->val) head = head->next;\n                prev->next = head->next;\n            } else prev = prev->next;\n            head = head->next;\n        }\n        return dummy.next;\n    }\n};` },
    83: { title: "Remove Duplicates from Sorted List", difficulty: "Easy", time: "O(n)", space: "O(1)", code: `struct ListNode{int val;ListNode*next;ListNode(int x):val(x),next(nullptr){}};\n\nclass Solution {\npublic:\n    ListNode* deleteDuplicates(ListNode* head) {\n        ListNode* cur = head;\n        while (cur && cur->next) {\n            if (cur->val == cur->next->val) cur->next = cur->next->next;\n            else cur = cur->next;\n        }\n        return head;\n    }\n};` },
    84: { title: "Largest Rectangle in Histogram", difficulty: "Hard", time: "O(n)", space: "O(n)", code: `#include <vector>\n#include <stack>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\npublic:\n    int largestRectangleArea(vector<int>& heights) {\n        stack<int> st; int maxA = 0, n = heights.size();\n        for (int i = 0; i <= n; i++) {\n            int h = (i == n) ? 0 : heights[i];\n            while (!st.empty() && heights[st.top()] >= h) {\n                int curH = heights[st.top()]; st.pop();\n                int w = st.empty() ? i : i - st.top() - 1;\n                maxA = max(maxA, curH * w);\n            }\n            st.push(i);\n        }\n        return maxA;\n    }\n};` },
    85: { title: "Maximal Rectangle", difficulty: "Hard", time: "O(m·n)", space: "O(n)", code: `#include <vector>\n#include <algorithm>\n#include <stack>\nusing namespace std;\n\nclass Solution {\npublic:\n    int maximalRectangle(vector<vector<char>>& matrix) {\n        if (matrix.empty()) return 0;\n        int m = matrix.size(), n = matrix[0].size(), res = 0;\n        vector<int> h(n, 0);\n        for (int i = 0; i < m; i++) {\n            for (int j = 0; j < n; j++) h[j] = (matrix[i][j] == '1' ? h[j] + 1 : 0);\n            res = max(res, largest(h));\n        }\n        return res;\n    }\n    int largest(vector<int>& heights) {\n        stack<int> st; int maxA = 0; heights.push_back(0);\n        for (int i = 0; i < (int)heights.size(); i++) {\n            while (!st.empty() && heights[st.top()] >= heights[i]) {\n                int h = heights[st.top()]; st.pop();\n                int w = st.empty() ? i : i - st.top() - 1;\n                maxA = max(maxA, h * w);\n            }\n            st.push(i);\n        }\n        heights.pop_back(); return maxA;\n    }\n};` },
    86: { title: "Partition List", difficulty: "Medium", time: "O(n)", space: "O(1)", code: `struct ListNode{int val;ListNode*next;ListNode(int x):val(x),next(nullptr){}};\n\nclass Solution {\npublic:\n    ListNode* partition(ListNode* head, int x) {\n        ListNode d1(0), d2(0); ListNode *p1 = &d1, *p2 = &d2;\n        while (head) {\n            if (head->val < x) { p1->next = head; p1 = p1->next; }\n            else { p2->next = head; p2 = p2->next; }\n            head = head->next;\n        }\n        p2->next = nullptr; p1->next = d2.next;\n        return d1.next;\n    }\n};` },
    87: { title: "Scramble String", difficulty: "Hard", time: "O(n⁴)", space: "O(n³)", code: `#include <string>\n#include <vector>\n#include <unordered_map>\nusing namespace std;\n\nclass Solution {\n    unordered_map<string, bool> memo;\npublic:\n    bool isScramble(string s1, string s2) {\n        if (s1 == s2) return true;\n        if (s1.size() != s2.size()) return false;\n        string key = s1 + "_" + s2;\n        if (memo.count(key)) return memo[key];\n        int n = s1.size();\n        vector<int> count(26, 0);\n        for (int i = 0; i < n; i++) { count[s1[i]-'a']++; count[s2[i]-'a']--; }\n        for (int c : count) if (c != 0) return memo[key] = false;\n        for (int i = 1; i < n; i++) {\n            if (isScramble(s1.substr(0, i), s2.substr(0, i)) && isScramble(s1.substr(i), s2.substr(i))) return memo[key] = true;\n            if (isScramble(s1.substr(0, i), s2.substr(n - i)) && isScramble(s1.substr(i), s2.substr(0, n - i))) return memo[key] = true;\n        }\n        return memo[key] = false;\n    }\n};` },
    88: { title: "Merge Sorted Array", difficulty: "Easy", time: "O(m+n)", space: "O(1)", code: `#include <vector>\nusing namespace std;\n\nclass Solution{\npublic:\n    void merge(vector<int>&nums1,int m,vector<int>&nums2,int n){\n        int i=m-1,j=n-1,k=m+n-1;\n        while(j>=0){\n            if(i>=0&&nums1[i]>nums2[j]) nums1[k--]=nums1[i--];\n            else nums1[k--]=nums2[j--];\n        }\n    }\n};` },
    89: { title: "Gray Code", difficulty: "Medium", time: "O(2ⁿ)", space: "O(2ⁿ)", code: `#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    vector<int> grayCode(int n) {\n        vector<int> res; for (int i = 0; i < (1 << n); i++) res.push_back(i ^ (i >> 1));\n        return res;\n    }\n};` },
    90: { title: "Subsets II", difficulty: "Medium", time: "O(n·2ⁿ)", space: "O(n·2ⁿ)", code: `#include <vector>\n#include <algorithm>\nusing namespace std;\n\nclass Solution {\n    void solve(vector<int>& n, int s, vector<int>& cur, vector<vector<int>>& res) {\n        res.push_back(cur);\n        for (int i = s; i < n.size(); i++) {\n            if (i > s && n[i] == n[i-1]) continue;\n            cur.push_back(n[i]); solve(n, i + 1, cur, res); cur.pop_back();\n        }\n    }\npublic:\n    vector<vector<int>> subsetsWithDup(vector<int>& nums) {\n        sort(nums.begin(), nums.end());\n        vector<vector<int>> res; vector<int> cur; solve(nums, 0, cur, res); return res;\n    }\n};` },
    91: { title: "Decode Ways", difficulty: "Medium", time: "O(n)", space: "O(1)", code: `#include <string>\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int numDecodings(string s) {\n        if (s.empty() || s[0] == '0') return 0;\n        int n = s.size(), p1 = 1, p2 = 1;\n        for (int i = 1; i < n; i++) {\n            int cur = 0;\n            if (s[i] != '0') cur = p1;\n            int two = stoi(s.substr(i - 1, 2));\n            if (two >= 10 && two <= 26) cur += p2;\n            p2 = p1; p1 = cur;\n        }\n        return p1;\n    }\n};` },
    92: { title: "Reverse Linked List II", difficulty: "Medium", time: "O(n)", space: "O(1)", code: `struct ListNode{int val;ListNode*next;ListNode(int x):val(x),next(nullptr){}};\n\nclass Solution {\npublic:\n    ListNode* reverseBetween(ListNode* head, int m, int n) {\n        ListNode dummy(0); dummy.next = head; ListNode* pre = &dummy;\n        for (int i = 0; i < m - 1; i++) pre = pre->next;\n        ListNode* start = pre->next; ListNode* then = start->next;\n        for (int i = 0; i < n - m; i++) {\n            start->next = then->next;\n            then->next = pre->next;\n            pre->next = then;\n            then = start->next;\n        }\n        return dummy.next;\n    }\n};` },
    93: { title: "Restore IP Addresses", difficulty: "Medium", time: "O(3⁴)", space: "O(1)", code: `#include <vector>\n#include <string>\nusing namespace std;\n\nclass Solution {\npublic:\n    vector<string> restoreIpAddresses(string s) {\n        vector<string> res; string ip;\n        for (int a = 1; a < 4; a++) for (int b = 1; b < 4; b++) for (int c = 1; c < 4; c++) for (int d = 1; d < 4; d++) {\n            if (a + b + c + d == s.size()) {\n                int A = stoi(s.substr(0, a)), B = stoi(s.substr(a, b)), C = stoi(s.substr(a + b, c)), D = stoi(s.substr(a + b + c, d));\n                if (A <= 255 && B <= 255 && C <= 255 && D <= 255) {\n                    if ((ip = to_string(A) + "." + to_string(B) + "." + to_string(C) + "." + to_string(D)).size() == s.size() + 3) res.push_back(ip);\n                }\n            }\n        }\n        return res;\n    }\n};` },
    94: { title: "Binary Tree Inorder Traversal", difficulty: "Easy", time: "O(n)", space: "O(n)", code: `#include <vector>\nusing namespace std;\nstruct TreeNode{int val;TreeNode*left,*right;TreeNode(int x):val(x),left(nullptr),right(nullptr){}};\n\nclass Solution {\n    void solve(TreeNode* root, vector<int>& res) {\n        if (!root) return;\n        solve(root->left, res); res.push_back(root->val); solve(root->right, res);\n    }\npublic:\n    vector<int> inorderTraversal(TreeNode* root) {\n        vector<int> res; solve(root, res); return res;\n    }\n};` },
    95: { title: "Unique Binary Search Trees II", difficulty: "Medium", time: "O(4ⁿ/n√n)", space: "O(4ⁿ/n√n)", code: `#include <vector>\nusing namespace std;\nstruct TreeNode{int val;TreeNode*left,*right;TreeNode(int x):val(x),left(nullptr),right(nullptr){}};\n\nclass Solution {\n    vector<TreeNode*> solve(int s, int e) {\n        if (s > e) return {nullptr};\n        vector<TreeNode*> res;\n        for (int i = s; i <= e; i++) {\n            auto left = solve(s, i - 1), right = solve(i + 1, e);\n            for (auto l : left) for (auto r : right) {\n                TreeNode* node = new TreeNode(i);\n                node->left = l; node->right = r; res.push_back(node);\n            }\n        }\n        return res;\n    }\npublic:\n    vector<TreeNode*> generateTrees(int n) { if (n == 0) return {}; return solve(1, n); }\n};` },
    96: { title: "Unique Binary Search Trees", difficulty: "Medium", time: "O(n²)", space: "O(n)", code: `#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    int numTrees(int n) {\n        vector<int> G(n + 1, 0); G[0] = G[1] = 1;\n        for (int i = 2; i <= n; i++) for (int j = 1; j <= i; j++) G[i] += G[j-1] * G[i-j];\n        return G[n];\n    }\n};` },
    97: { title: "Interleaving String", difficulty: "Medium", time: "O(m·n)", space: "O(n)", code: `#include <string>\n#include <vector>\nusing namespace std;\n\nclass Solution {\npublic:\n    bool isInterleave(string s1, string s2, string s3) {\n        if (s1.size() + s2.size() != s3.size()) return false;\n        int m = s1.size(), n = s2.size();\n        vector<bool> dp(n + 1, false);\n        for (int i = 0; i <= m; i++) for (int j = 0; j <= n; j++) {\n            if (i == 0 && j == 0) dp[j] = true;\n            else if (i == 0) dp[j] = dp[j-1] && s2[j-1] == s3[i+j-1];\n            else if (j == 0) dp[j] = dp[j] && s1[i-1] == s3[i+j-1];\n            else dp[j] = (dp[j] && s1[i-1] == s3[i+j-1]) || (dp[j-1] && s2[j-1] == s3[i+j-1]);\n        }\n        return dp[n];\n    }\n};` },
    98: { title: "Validate Binary Search Tree", difficulty: "Medium", time: "O(n)", space: "O(h)", code: `#include <climits>\nstruct TreeNode{int val;TreeNode*left,*right;TreeNode(int x):val(x),left(nullptr),right(nullptr){}};\n\nclass Solution {\n    bool solve(TreeNode* root, long minV, long maxV) {\n        if (!root) return true;\n        if (root->val <= minV || root->val >= maxV) return false;\n        return solve(root->left, minV, root->val) && solve(root->right, root->val, maxV);\n    }\npublic:\n    bool isValidBST(TreeNode* root) {\n        return solve(root, LONG_MIN, LONG_MAX);\n    }\n};` },
    99: { title: "Recover Binary Search Tree", difficulty: "Medium", time: "O(n)", space: "O(h)", code: `#include <algorithm>\nusing namespace std;\nstruct TreeNode{int val;TreeNode*left,*right;TreeNode(int x):val(x),left(nullptr),right(nullptr){}};\n\nclass Solution {\n    TreeNode *f = nullptr, *s = nullptr, *pre = new TreeNode(INT_MIN);\n    void solve(TreeNode* root) {\n        if (!root) return;\n        solve(root->left);\n        if (f == nullptr && pre->val >= root->val) f = pre;\n        if (f != nullptr && pre->val >= root->val) s = root;\n        pre = root; solve(root->right);\n    }\npublic:\n    void recoverTree(TreeNode* root) { solve(root); swap(f->val, s->val); }\n};` },
    100: { title: "Same Tree", difficulty: "Easy", time: "O(n)", space: "O(h)", code: `struct TreeNode{int val;TreeNode*left,*right;TreeNode(int x):val(x),left(nullptr),right(nullptr){}};\n\nclass Solution{\npublic:\n    bool isSameTree(TreeNode*p,TreeNode*q){\n        if(!p&&!q) return true;\n        if(!p||!q||p->val!=q->val) return false;\n        return isSameTree(p->left,q->left)&&isSameTree(p->right,q->right);\n    }\n};` }
};
